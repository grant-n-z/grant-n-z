name: grant_n_z_ci
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: setup-go
      uses: actions/setup-go@v1
      with:
        go-version: 1.13
      id: go
    - name: unit-test
      run: ls -la
